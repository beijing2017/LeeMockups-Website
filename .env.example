# Product previews are served by the Worker, not by a public R2 bucket.
NEXT_PUBLIC_ASSET_BASE_URL=https://downloads.leemockups.com
ASSET_BASE_URL=https://downloads.leemockups.com
R2_BUCKET=leemockups-files
# Optional absolute paths when binaries are not on PATH:
# FFMPEG_PATH=C:\path\to\ffmpeg.exe
# WRANGLER_PATH=C:\path\to\wrangler.cmd
